# 如何实现 jscpython.github.io 根域名

## 📌 核心原理

GitHub Pages 有一个特殊规则：
- **当仓库名称是 `你的用户名.github.io` 时**，网站会直接部署到 `https://你的用户名.github.io/`（根域名）
- **当仓库名称是其他名称时**，网站会部署到 `https://你的用户名.github.io/仓库名/`（子路径）

## 🎯 实现步骤

### 第一步：在 GitHub 创建特殊命名的仓库

1. **访问 GitHub 并登录**
   - 打开：https://github.com
   - 使用你的账号登录（用户名：`jscpython`）

2. **创建新仓库**
   - 点击右上角的 "+" 号
   - 选择 "New repository"

3. **设置仓库名称（关键步骤）**
   - **仓库名称必须填写**：`jscpython.github.io`
   - ⚠️ **重要**：仓库名必须是 `你的用户名.github.io` 格式，不能有其他字符
   - 描述（可选）：填写 "爱壹帆网站"
   - 选择 Public（公开）或 Private（私有）
   - **不要**勾选 "Initialize this repository with a README"
   - 点击 "Create repository"

### 第二步：推送代码到新仓库

#### 方式 A：如果当前项目还没有推送到 GitHub

```bash
# 1. 进入项目目录
cd "/Users/jessica/Desktop/github/爱一帆"

# 2. 检查 Git 状态（如果还没初始化）
git status

# 3. 如果还没有提交，先提交代码
git add .
git commit -m "Initial commit: 爱壹帆网站"

# 4. 设置主分支名称
git branch -M main

# 5. 添加远程仓库（使用特殊命名的仓库）
git remote add origin https://github.com/jscpython/jscpython.github.io.git

# 6. 推送代码
git push -u origin main
# 推送时会要求身份验证：
# - 用户名：jscpython
# - 密码：使用个人访问令牌（不是 GitHub 密码）
```

#### 方式 B：如果当前项目已经推送到 hamer 仓库

```bash
# 1. 进入项目目录
cd "/Users/jessica/Desktop/github/爱一帆"

# 2. 更改远程仓库地址
git remote set-url origin https://github.com/jscpython/jscpython.github.io.git

# 3. 推送代码到新仓库
git push -u origin main
# 推送时会要求身份验证：
# - 用户名：jscpython
# - 密码：使用个人访问令牌（不是 GitHub 密码）
```

### 第三步：启用 GitHub Pages（通常自动启用）

1. **访问仓库设置**
   - 打开：https://github.com/jscpython/jscpython.github.io/settings/pages

2. **检查 Pages 设置**
   - 如果仓库名是 `jscpython.github.io`，GitHub Pages 通常会自动启用
   - Source 应该显示：`Deploy from a branch`
   - Branch 应该显示：`main` 或 `master`
   - Folder 应该显示：`/ (root)`

3. **如果未自动启用**
   - 手动选择 "Deploy from a branch"
   - Branch 选择 "main"
   - Folder 选择 "/ (root)"
   - 点击 "Save"

### 第四步：等待部署完成

1. **查看部署状态**
   - 访问：https://github.com/jscpython/jscpython.github.io/actions
   - 可以看到部署进度

2. **等待 1-2 分钟**
   - GitHub 需要时间构建和部署网站
   - 通常 1-2 分钟后即可访问

3. **访问网站**
   - 打开：https://jscpython.github.io/
   - ✅ 成功！你现在有了根域名

## 🔍 验证方法

### 检查仓库名称是否正确

```bash
# 查看远程仓库地址
git remote -v

# 应该显示：
# origin  https://github.com/jscpython/jscpython.github.io.git (fetch)
# origin  https://github.com/jscpython/jscpython.github.io.git (push)
```

### 检查网站是否部署成功

1. **访问仓库页面**
   - https://github.com/jscpython/jscpython.github.io
   - 应该能看到你的代码文件

2. **访问网站**
   - https://jscpython.github.io/
   - 应该能看到你的网站内容

3. **检查 Pages 设置**
   - https://github.com/jscpython/jscpython.github.io/settings/pages
   - 应该显示绿色的成功提示和网站地址

## ⚠️ 重要注意事项

1. **每个账号只能有一个根域名仓库**
   - 每个 GitHub 账号只能创建一个 `用户名.github.io` 仓库
   - 如果已经存在，需要删除或重命名旧的仓库

2. **仓库名称必须完全匹配**
   - 必须是 `jscpython.github.io`（完全匹配，区分大小写）
   - 不能是 `Jscpython.github.io` 或 `jscpython.GitHub.io`

3. **主分支名称**
   - 可以是 `main` 或 `master`
   - 确保在 Pages 设置中选择正确的分支

4. **文件结构**
   - `index.html` 必须在仓库根目录
   - 资源文件路径使用相对路径（如 `./files/xxx`）

## 🆚 对比：根域名 vs 子路径域名

| 特性 | 根域名 | 子路径域名 |
|------|--------|------------|
| 仓库名 | `jscpython.github.io` | `hamer`（任意名称） |
| 网站地址 | `https://jscpython.github.io/` | `https://jscpython.github.io/hamer/` |
| 优点 | 更简洁、专业 | 可以创建多个项目站点 |
| 限制 | 每个账号只能有一个 | 可以创建多个 |

## 🐛 常见问题

### Q1: 推送时提示 "repository not found"
**原因**：仓库还没有创建，或者仓库名称不正确
**解决**：先在 GitHub 网页上创建 `jscpython.github.io` 仓库

### Q2: 推送时提示身份验证失败
**原因**：GitHub 不再支持密码推送
**解决**：使用个人访问令牌（Personal Access Token）
- 创建令牌：https://github.com/settings/tokens
- 勾选 `repo` 权限
- 推送时使用令牌作为密码

### Q3: 网站显示 404
**原因**：部署还未完成，或文件路径错误
**解决**：
- 等待 1-2 分钟
- 检查 `index.html` 是否在根目录
- 检查资源文件路径是否为相对路径

### Q4: 想从 hamer 切换到根域名
**解决**：
1. 创建新仓库 `jscpython.github.io`
2. 更改远程地址：`git remote set-url origin https://github.com/jscpython/jscpython.github.io.git`
3. 推送代码：`git push -u origin main`

## 📚 参考资源

- [GitHub Pages 官方文档](https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site)
- [创建个人网站](https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site#creating-your-site)

