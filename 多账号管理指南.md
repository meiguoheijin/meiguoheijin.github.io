# GitHub 多账号管理指南

## 📋 当前配置

当前 Git 配置：
- 用户名：`jscpython`
- 邮箱：`gusaomeishi@gmail.com`
- 远程仓库：`git@github.com:jscpython/hamer.git`

## 🔄 方法一：临时切换当前项目的 Git 配置（推荐）

如果你只想为这个项目使用不同的账号：

```bash
cd "/Users/jessica/Desktop/github/爱一帆"

# 设置当前项目的用户名和邮箱（只影响这个仓库）
git config user.name "你的GitHub用户名"
git config user.email "你的GitHub邮箱"

# 查看是否更改成功
git config user.name
git config user.email
```

**优点**：只影响当前项目，不影响其他项目

## 🔄 方法二：全局切换 Git 配置

如果你想全局更改 Git 配置：

```bash
# 设置全局用户名和邮箱
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub邮箱"

# 查看全局配置
git config --global user.name
git config --global user.email
```

**注意**：这会影响所有 Git 仓库

## 🔐 方法三：使用 SSH 密钥管理多账号（最佳实践）

### 步骤 1：为每个账号生成 SSH 密钥

```bash
# 为第一个账号生成密钥（如果还没有）
ssh-keygen -t ed25519 -C "账号1的邮箱" -f ~/.ssh/id_ed25519_account1

# 为第二个账号生成密钥
ssh-keygen -t ed25519 -C "账号2的邮箱" -f ~/.ssh/id_ed25519_account2
```

### 步骤 2：配置 SSH config 文件

编辑或创建 `~/.ssh/config` 文件：

```bash
# 编辑 SSH config
nano ~/.ssh/config
# 或
code ~/.ssh/config
```

添加以下内容：

```
# 账号1配置
Host github.com-account1
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_account1

# 账号2配置
Host github.com-account2
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_account2
```

### 步骤 3：将 SSH 公钥添加到 GitHub

```bash
# 查看公钥内容
cat ~/.ssh/id_ed25519_account1.pub
cat ~/.ssh/id_ed25519_account2.pub
```

然后：
1. 登录对应的 GitHub 账号
2. 访问：https://github.com/settings/keys
3. 点击 "New SSH key"
4. 粘贴对应的公钥内容

### 步骤 4：为不同项目使用不同的 SSH Host

```bash
cd "/Users/jessica/Desktop/github/爱一帆"

# 如果使用账号1，使用这个 Host
git remote set-url origin git@github.com-account1:用户名/仓库名.git

# 如果使用账号2，使用这个 Host
git remote set-url origin git@github.com-account2:用户名/仓库名.git
```

## 🔑 方法四：使用 HTTPS + 个人访问令牌

### 步骤 1：为每个账号创建个人访问令牌

1. 登录对应的 GitHub 账号
2. 访问：https://github.com/settings/tokens
3. 创建新令牌，勾选 `repo` 权限
4. 复制令牌（只显示一次）

### 步骤 2：使用 HTTPS 推送

```bash
cd "/Users/jessica/Desktop/github/爱一帆"

# 设置当前项目的用户名和邮箱
git config user.name "账号的用户名"
git config user.email "账号的邮箱"

# 使用 HTTPS 推送
git remote set-url origin https://github.com/用户名/仓库名.git
git push -u origin main

# 推送时会要求输入：
# - 用户名：GitHub 用户名
# - 密码：粘贴个人访问令牌
```

### 步骤 3：使用 Git Credential Helper（可选）

保存凭据，避免每次输入：

```bash
# macOS
git config --global credential.helper osxkeychain

# 推送一次后，凭据会保存在钥匙串中
```

## 🎯 针对当前项目的快速切换

### 如果你想使用不同的账号部署这个项目：

```bash
cd "/Users/jessica/Desktop/github/爱一帆"

# 1. 设置当前项目的 Git 配置
git config user.name "新的GitHub用户名"
git config user.email "新的GitHub邮箱"

# 2. 更改远程仓库地址（使用 HTTPS）
git remote set-url origin https://github.com/新的用户名/仓库名.git

# 3. 或者使用 SSH（如果配置了多账号 SSH）
git remote set-url origin git@github.com-account2:新的用户名/仓库名.git

# 4. 查看配置
git config user.name
git config user.email
git remote -v
```

## 📝 检查当前配置

```bash
# 查看当前项目的配置
cd "/Users/jessica/Desktop/github/爱一帆"
git config user.name
git config user.email
git remote -v

# 查看全局配置
git config --global user.name
git config --global user.email
```

## ⚠️ 注意事项

1. **每个账号的仓库名**
   - 账号1：`账号1用户名.github.io`
   - 账号2：`账号2用户名.github.io`
   - 每个账号只能有一个根域名仓库

2. **SSH vs HTTPS**
   - SSH：需要配置密钥，但更安全方便
   - HTTPS：需要每次输入令牌，但配置简单

3. **个人访问令牌**
   - 每个账号需要单独创建令牌
   - 令牌只显示一次，请妥善保存

4. **项目级配置优先**
   - 项目级配置会覆盖全局配置
   - 使用 `git config`（不加 `--global`）只影响当前项目

## 🚀 推荐方案

**对于多账号管理，推荐使用：**
1. **SSH 密钥 + SSH config**（最方便，一次配置长期使用）
2. **项目级 Git 配置**（每个项目使用不同账号）

**对于当前项目：**
- 先确定要使用哪个 GitHub 账号
- 设置项目级 Git 配置
- 使用 HTTPS + 个人访问令牌推送（最简单）

